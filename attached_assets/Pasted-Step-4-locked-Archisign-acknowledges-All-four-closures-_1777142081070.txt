Step 4 — locked. Archisign acknowledges.
All four closures recorded on Archisign's side:

G11 disposition (lot-scoped + case-insensitive label-match for v1.0): noted; no Archisign coupling — purely an Architrak↔Archidoc concern.
Archidoc adjustment delta on #331/#332/#333 with #329/#330 untouched: noted.
Reverse-deps in §1.3 reflecting the tightened §C: matches what was sent. AT2 added to AS3 dependents (E2E only — Week 3); AT3 dropped from AS4 (the conflation is fixed).
G6 generation-algorithm asymmetry (UUIDv7 for AS3 + AT5 vs deterministic ${incidentRef}:${envelopeId} for Archidoc's Archisign-direct re-mint path): noted; correctly reasoned — generation sources never collide at the same dedup key because event_source distinguishes them. No interop impact.
Handoff to project-task creation
The five Archisign tasks proposed on 2026-04-25 are still in Drafts state under the dependency chain AS1 → AS2 → {AS3, AS4} → AS5. Per the §5 operational note:

Day 0: AS1 schema migration is the only task that can run truly disjoint from anything else in the entire three-app system (parallel-safe with AD1 and AT1). Everything downstream of AS1 within Archisign shares files (server/routes.ts near the 1,000-line cap, server/services/NotificationService.ts, shared/schema.ts) and should run dependency-chained, not parallel.
Weeks 1–4: AS2 → AS3/AS4 → AS5 land sequentially on the main branch. The dependency declarations in the proposed tasks already enforce correct order; if isolated agents are used instead, the same chain prevents merge conflicts on the shared files.
Awaiting approval on the five Drafts (Task #1 schema foundation through Task #5 background jobs) to begin the Day 0 → Week 5 schedule. No further planning artefacts to produce; v1.0 contract + Step 4 breakdown are both frozen and live in Architrak's repo as the canonical references.

Subsequent contract amendments will follow the v1.1 cycle through the same protocol when needed.

No code or schema changes have been made — Archisign remains in planning posture, awaiting the green light on the five proposed tasks.