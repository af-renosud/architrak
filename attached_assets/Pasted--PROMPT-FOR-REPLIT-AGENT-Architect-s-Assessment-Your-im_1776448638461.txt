[PROMPT FOR REPLIT AGENT]

Architect's Assessment:
Your implementation of `server/env.ts` is approved and meets production standards. The fail-fast mechanics and secret redaction are spot on. 

However, we have an immediate blocker: the 21 baseline `tsc` errors. Absolute code purity means our pipeline must compile cleanly. We are also advancing to Phase 3: Security & Persistence Hardening, beginning with our API boundary.

Directive 2: Zero-Tolerance TypeScript Audit & Global Error Hardening

Execute the following steps sequentially and precisely:

### Step 1: Eradicate All TypeScript Errors
1. Run `npx tsc --noEmit` and systematically resolve every single error.
2. **Client `StatusBadge` Props:** Correct the interface definitions. If the Shadcn UI components have loose typing, strict them up. Do not bypass with `as any`.
3. **`tvaRate` Schema Drift:** Synchronize the frontend/backend schemas. The database schema in `shared/schema.ts` must be the absolute source of truth. Ensure Zod schemas exactly match Drizzle types.
4. **Vendored Integrations / Gemini `EnumStringSchema`:** If external or generated types are broken, write type-safe adapter functions or proper module augmentations (`declare module ...`). 
5. You are strictly forbidden from using `@ts-ignore`, `@ts-expect-error`, or `as any`. If a type is unknown, use `unknown` and validate it with Zod.

### Step 2: Secure Global Error Handling
Since you are on Express 5, unhandled promise rejections in routes will correctly fall through to the global error handler. We must ensure this handler is secure.

1. Create a new middleware file: `server/middleware/error-handler.ts`.
2. Implement an Express error handling middleware with the signature: `(err: any, req: Request, res: Response, next: NextFunction)`.
3. **Internal Logging:** Log the full error stack to `console.error` (or your logger) for internal tracing, including the Request ID if available.
4. **Zod Errors:** If the error is an instance of `z.ZodError`, intercept it and return a `400 Bad Request`. Format the payload to cleanly array the validation issues (e.g., path and message) without exposing backend logic.
5. **Production Masking:** For all other errors, check `env.NODE_ENV`. 
   - If `production`, return a generic `500 Internal Server Error` with a standardized JSON structure (e.g., `{ error: "Internal Server Error", code: "INTERNAL_ERROR" }`). **Never leak stack traces or raw database error strings to the client.**
   - If `development`, you may return the stack trace in the JSON response to aid debugging.
6.